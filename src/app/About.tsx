import { Text } from 'react-native';
import { View } from '../components/Themed';

export default function About() {
  return (
    <View style={{justifyContent:'center'}}>
    <Text style={{justifyContent:'center',color:"white"}}>About</Text>
    </View>
  );
}